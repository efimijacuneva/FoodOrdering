apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: food-ordering
  labels:
    app: food-ordering
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard
